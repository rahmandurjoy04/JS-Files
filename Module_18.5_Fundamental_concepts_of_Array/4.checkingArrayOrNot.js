const names = ['rohim', 'korim', 'jodu'];
const empty = [];
const name = 'Rohim Er Boro Bhai Korim'

// Checking a variable is array or not
console.log(Array.isArray(names))
console.log(Array.isArray(empty))
console.log(Array.isArray(name))