const names = ['Arif', 'Forhad', 'faruk', 'talukder'];
const fruits = ['apple', 'banana', 'orange']
const newArray = names.concat(fruits)

// The first array is
console.log(names)

// The second array is
console.log(fruits)

// The final array is
console.log(newArray)